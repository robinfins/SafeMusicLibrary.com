import React from 'react'
import ReactDOM from 'react-dom/client'
import SafeMusicLibrary from './App'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SafeMusicLibrary />
  </React.StrictMode>
)
